Vagrant.configure("2") do |config|
  config.vm.box = "bitroid/rockylinux9"
  config.vm.box_version = "6.0.0"
  config.vm.provision "shell", inline: <<-SHELL
    dnf install -y nginx
    systemctl enable nginx
    systemctl start nginx
    systemctl status nginx
  SHELL
end